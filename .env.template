# A unique random string used to encrypt backup archives
BACKUP_ENCRYPTION_KEY=

# Git Backup Destination: Remote repository details for automated sync
BACKUP_GIT_REPO=
BACKUP_GIT_BRANCH=
BACKUP_GIT_ACCESS_TOKEN=

# A unique random strings, N8N_ENCRYPTION_KEY after setup will break existing credentials.
N8N_ENCRYPTION_KEY=
N8N_RUNNERS_AUTH_TOKEN=

# Internal PostgresSQL credentials
POSTGRES_DB=
POSTGRES_DB_USER=
POSTGRES_DB_PASSWORD=
